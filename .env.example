# 复制为 .env 并按需填写。本文件全部为占位示例,不含真实地址与凭据。
# 认证在 nginx 层(basic auth,凭据在服务器 /etc/nginx/*.htpasswd),应用不读取账号密码。

# 功能开关初始默认值(默认全开):置 0 关闭。也可在「设置页 → 功能开关」即时切换
# (改动存数据库,优先于本文件)。关闭后菜单隐藏、对应页面 404,不影响其他功能;
# 单个模块依赖缺失或加载失败时也只跳过该模块。
# ENABLE_GEN=1       # 生成页与工作流模板
# ENABLE_GALLERY=1   # 画廊与收藏
# ENABLE_CIVITAI=1   # 模型/LoRA 页(Civitai)
# ENABLE_AI=1        # AI 润色/翻译/tag 联想
# ENABLE_BATCH=1     # 批量生成

# LiteGate AI 网关(提示词助手「AI 润色/翻译」+ 批量生成「AI 细化提示词」)
# LITEGATE_BASE=https://192.168.x.x:29007/v1
# LITEGATE_MODEL=nvidia/nemotron-3-super-120b-a12b:free
# LITEGATE_API_KEY=sk-xxx
# 网关用自签证书时打开(跳过证书校验,仅限内网)
# LITEGATE_INSECURE=1

# ComfyUI 主机的 GPU 温度/利用率跨机采集(批量生成页与设置页展示):
# GPU 主机开启 SSH 并部署本机公钥后配置,如 COMFY_SSH=youruser@192.168.x.x
# 首次连接先手动录入主机密钥(应用不做首次自动信任):
#   ssh-keyscan -p 22 <GPU主机IP> >> ~/.ssh/known_hosts
# COMFY_SSH=

# 批量生成引擎的 GPU 温度保护(默认关)。
# 仅当本服务与 ComfyUI 同在一台 GPU 主机时才有意义(温度经本机 nvidia-smi 读取);
# ≥72°C 休 60s、≥78°C 休 180s 后继续。
# BATCHGEN_GPU_TEMP=1
